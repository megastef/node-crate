sudo: required

language: node_js
node_js:
  - "node"
  - "6"
  - "6.1"

services:
  - docker

before_install:
  - docker pull crate
  - docker run -d -p 4200:4200 -p 4300:4300 crate

script:
  - npm test